# F08-05 原会话真实模型复验：未通过

## 补充：空白会话 4 次原句测试已完成

当前生产版本、空聊天历史、相同个人偏好下，4/4 文件名未加空格；明确工作区的 2 次找到目标，截图原句的 2 次因省略 scope 落入空附件范围。未执行生产删除。不能据旧会话失败断言新会话必然失败；结构化绑定仍是建议，暂不作为已确定的下一实施方向。[方法边界与原始证据](TOOL_UNIFICATION_FRESH_CONVERSATION_08.md)。


2026-09-13 23:44–23:46（Asia/Shanghai），用户明确“增加”授权后执行新增 24 次 DashScope 调用，额度已全部用完；没有重试、强制工具选择或隐藏失败。生产业务 Handler 调用 0、业务写入 0，原 Excel inode/大小/mtime 未变。未部署、合并或清理工作树。

**结论：当前文件协议候选真实模型验收未通过，不可发布为已修复。** 本地相关测试通过仍有效，但不能替代真实模型行为。

## 固定输入和版本

- 服务源码/配置指纹及发布标记核对为 `8428b72c0c8b36481e5fdbc3bcb3e93070c2f1f9`；候选为该 HEAD 加本任务未提交差异，逐文件 [SHA256](tool-unification-evidence/08-file-protocol/tested-sources.json) 前后相同。
- 原失败第一个 file_search 之前的 6 条消息原样复用，最新 user 文件名无空格，未注入失败后的错误名字历史。实际生产 Gateway/DashScope adapter，qwen3.5-plus，11 个生产工具；只有 file_search 定义发生实验替换，其他 10 个工具相同。
- 按真实生产 `_read_turn` 发送 stream/include_usage、enable_thinking=false；没有额外设置 temperature/top_p/top_k/max_tokens/tool_choice。模型别名不提供不可变权重版本，不能声称控制住服务端所有变化。
- 候选后 8 个场景在执行前冻结：关键词、完整路径、合法空格、重名、范围不明确、附件 ID、搜索引用删除、搜索引用分析。前 6 项是在原系统消息下替换成合成用户请求，后两项保留/重建目标请求并注入现有 formatter 的真实搜索引用行。这些是合成边界样本，不能冒充原会话生产完整回放。
- 原会话/真实目标返回参数由同一用户范围内只读 resolver/query 核验。候选捕获参数另经本地候选 prepare/Handler 在临时目录合成文件核验；删除/分析引用只检查原样传递，不进入业务执行。确认版本、拒绝/超时及实际临时文件删除依赖此前本地三入口测试。

## 第一阶段：单变量说明对照（12 次）

| 说明 | 次数 | 名字加空格 | 实际只读命中 |
|---|---:|---:|---:|
| 当前生产定义 | 4 | 2 | 2 |
| 仅删搜索定义中的分析长指引 | 4 | 3 | 1 |
| 仅修正 path 参数说明，写明完整文件名原样复制 | 4 | 0 | 4 |

12 次均使用 keyword、均带 workspace。后者虽命中，仍没有进入期望的完整名字定位路径。小样本支持定义内容会影响结果，不能认定某一句是唯一原因，也不能用 4/4 承诺稳定正确。仅删分析长说明不足以解决。

[汇总与原始记录哈希](tool-unification-evidence/08-file-protocol/production-protocol-diagnosis-summary.json)。本阶段 provider usage：prompt 118912、completion 651、total 119563 tokens。

## 第二阶段：已实现候选（12 次）

| 固定场景 | 模型实际行为 | 判断 |
|---|---|---|
| 原会话 1、2、4 | 加空格的完整名字传 keyword；真实只读查找 0 命中 | 未通过 |
| 原会话 3 | 加空格的名字传 path；现有定位器找到唯一真实文件 | 通过本次定位；不证明模型原样复制 |
| 关键词 c7a21c | keyword + workspace，真实文件命中 | 通过 |
| 完整相对路径 | 修改了路径内文件名，严格路径定位未找到 | 未通过；未降级到同名文件，安全边界保持 |
| 合法空格文件名 | path 原样复制，候选优先精确名字 | 通过（本地合成文件） |
| 重名文件 | path 指定 basename，候选返回 RESOURCE_AMBIGUOUS 和两条路径 | 通过定位器歧义边界；未另调用模型验证它随后发问 |
| 范围不明确 | 未询问，直接 keyword + current | 未通过功能预期；没有扩大工作区权限 |
| 当前附件 ID | 正确 file_analyze.file_id，候选解析到本轮附件 | 通过选择器；未实际治理文件 |
| 搜索引用删除 | 正确 file_delete.resource_refs，完整引用未改写 | 通过选择器；未执行删除或真实确认弹窗 |
| 搜索引用分析 | 正确 file_analyze.resource_ref，完整引用未改写 | 通过选择器；未执行文件分析 |

共 **7 满足本次边界预期 / 5 未通过**，不是 7 次完整业务成功。12 次 HTTP 200 不等于功能通过。模型的所有 search 调用都满足新 required scope，但填写 scope 不等于理解正确。

[逐例判断](tool-unification-evidence/08-file-protocol/production-protocol-candidate-evaluation.json)、[请求用量/原始记录哈希](tool-unification-evidence/08-file-protocol/production-protocol-candidate-summary.json)。本阶段 prompt 116499、completion 1018、total 117517；本轮总 usage **237080 tokens**，不据此推算实际扣款。生产调用原始消息、请求及 SSE 保存在当前用户可读的外部证据目录，未纳入仓库。

## 问题归属与下一修复任务

F08-05 / 04（目标解析合同），05 配合模型说明：当前“让模型重新填写名字/路径”的输入方式仍允许语义重写；Handler 无法从被改写的 keyword 或严格路径可靠判断原始目标。已知 ID/引用样本正确，说明已有后半段机制可复用，但不能把本次三个选择器样本理解为零错误保证。

按已确认方案的失败分支，停止当前候选发布及额外模型调用，保留代码和反例，不追加关键词去空格或回退严格路径检查。下一最小设计任务为：在最新用户输入/附件身份入口，把原始文件名或原文片段建立成可验证的结构化选择器，再交由原 FileTargetResolver、资源范围、fid/resource_ref 和确认版本机制处理。模型选择引用而非重新生成名字；多名字/重名/范围不明仍需澄清。必须先明确这一选择器的公共合同与旧载荷兼容，再实施；当前不擅自新增字段、数据库或正则意图解析。

首批回归保留此次真实失败参数，增加原文绑定到定位器的端到端验证；字段完成后以新确定版本重跑必要本地及真实模型样本，需要新的模型调用额度。当前代码未完成上述结构化绑定。

技术状态：F08-05 未通过，F08-03/F08-04 仍未关闭，整体验收未通过。用户验收：当前候选未部署、未验收。受控关闭：未执行。
