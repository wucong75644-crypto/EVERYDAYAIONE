{
  "permissions": {
    "allow": [
      "Bash(npm run test:*)"
    ]
  },
  "hooks": {
    "afterEdit": [
      {
        "matcher": "frontend/**/*.{ts,tsx}",
        "command": "cd frontend && npx tsc --noEmit --pretty 2>&1 | head -20",
        "timeout": 15000
      }
    ]
  }
}
