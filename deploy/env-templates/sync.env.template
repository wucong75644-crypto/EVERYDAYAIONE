# Install as backend/.env.sync with mode 0600 during the staged role cutover.
# Sync/ERP remains on the legacy role until its own RLS domain is implemented.
DATABASE_URL=postgresql://everydayai:<legacy-password>@127.0.0.1:5432/everydayai
