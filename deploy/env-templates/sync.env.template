# Install as backend/.env.sync with mode 0600 during the staged role cutover.
# Sync/ERP uses an isolated login role without owner membership or BYPASSRLS.
DATABASE_URL=postgresql://everydayai_sync:<sync-password>@127.0.0.1:5432/everydayai
