# KIE 图片临时空间单次重试

## 边界

当前本地候选恢复“CDN 首次生成 + 海外后台旁路 + 400 单次临时链接重试”。以最新稳定基座 b41ebd69 为基础，参考历史 8454bac1 的客户端函数恢复，不回退整个提交或其他新功能。仅修改 KIE 客户端业务实现，保留现有调用签名、重试 service/request、缓存协议、Handler、积分、前端、代理配置。已删除的国内上传旁路不恢复。

## 请求与上传

`KieClient.create_task` 先将原 CDN 图片链接与原模型/参数经正常 7890 出口提交。成功取得真实 KIE task_id 后，后台启动一条海外旁路，立即返回首次受理结果；首次提交连接重发不重复启动上传。首次拒绝或没有任务号时不启动旁路，也不写上传缓存。没有参考图或非图片模型不启动旁路。

海外旁路恢复逐张处理：下载客户端继承原 HTTP_PROXY/HTTPS_PROXY（生产7890），下载 CDN 返回的实际图片字节；上传客户端显式使用 `KIE_SHADOW_OVERSEAS_PROXY`（生产7891），不继承环境代理。保留原下载连接10秒/读取60秒与上传连接10秒/读写90秒的超时，以及流式下载30MB上限。两客户端分离，KIE认证头不会发给CDN。不再读取当前用户工作区文件；保留客户端身份参数用于兼容已有调用方，但不参与本地文件读取。

完整原URL用于下载与缓存映射，保留签名、图片处理及版本参数，不用本地当前原文件替换CDN实际返回的图片。按首次请求去重后的原顺序逐张下载上传；只有所有参考图成功后才缓存整组临时链接，重试重放时恢复重复位置、保持原模型及全部参数。任意单图失败继续处理其他图片，但不缓存不完整链接供兜底使用。缺少海外代理变量时跳过旁路，不降级为国内上传。

旁路独立于首次任务：不再有首次提交前60秒整组等待，不因首次返回或主客户端关闭取消后台上传。主任务已经结束后旁路完成也只写短期缓存，不主动生成或重新结算。400兜底里的最多60秒等待保持不变，不是旁路上传的整组时限。

上传日志直接携带原始真实 KIE task_id，不再产生 `preupload-*` 或 `KIE_IMAGE_INPUT_BOUND`。缓存 pending/ready/failed、完整URL映射和原请求快照，一小时过期；快照不含回调地址、密钥。正常图片生成仍使用7890，临时空间上传仍使用7891。发布前生产仍使用前置上传版本，不能提前切换生产统计口径；发布后统计应按 `KIE_SHADOW_UPLOAD_SCHEDULED/SUCCESS/FAILURE/SUMMARY/FALLBACK_READY` 的真实任务号关联。

## 状态与一致性

独立 KieImageFallbackService 在 TaskCompletionService 失败结算前运行。request_params 内记录 waiting/submitting/submitted/failed、原任务号及等待截止时间；不迁移数据库。等待上传窗口 60 秒。现有回调模式默认 120 秒轮询，故模块内仅短暂轮询上传缓存；就绪、失败或到期后通知原完成入口一次，随后退出。进程重启后原轮询可续接 waiting 阶段，不改变全局频率。

沿用完成处理锁和数据库 version 条件更新，提交前持久化 attempted/submitting 标记。经正常 `7890 → api.kie.ai → overseas-ss` 提交一次，限制 60 秒；不再次启动上传旁路。成功取得新任务号后更新同一本地任务的 external_task_id。

请求超时、非 JSON/缺少任务号响应或 5xx 不盲重发，进入原最终失败处理。如果 KIE 已受理而任务号绑定失败，日志记录本地、原 KIE、新 KIE 任务号及 manual_review；不增加自动补绑定。数据库持续不可用时保留已落库的 submitting 标记，待原完成入口可继续处理时终结，不再提交。

明确取舍：上述跨系统异常需要人工核查，可能无法自动取回 KIE 已生成的结果；不保证外部受理与本地写库的原子性。删除本轮尚未发布的回执恢复缓存、内存回执、180 秒恢复循环及跨进程文件锁，不保留它们的局部残余。

复用本地任务、占位符及原积分锁定；最终成功确认一次，失败退款一次。等待与重提交期间不发失败事件。新生成重新计时。已绑定外部任务号的 KIE 图片超时只走统一完成处理，不允许旧任务快照直接标失败或退款；锁内重新核对阶段与时间。未绑定任务号的首次提交保留既有超时清理。专用重试最终失败不能再进入智能换模型。启动孤儿清理保留专用重试，由原完成入口结算，其他任务行为不变。

## 修改与移除

- 独立 service/request 两个模块：资格、参数重放、短暂等待、单次提交与超时判定。
- task_completion_service：失败分流、锁内超时保护。
- async_retry_service：移除迁出的 KIE 特例，保留 smart retry。
- KIE client：CDN首次提交，受理后后台下载CDN图片并经海外出口上传；缓存完整临时链接供原400单次重试使用。
- KIE adapter：参数整理不变；提供单次、不上传的内部重放入口。
- shadow_upload_store：沿用一小时上传缓存，增加状态和请求快照；无新提交恢复缓存。
- background_task_worker：KIE 图片不使用旧快照直接超时结算。
- task_recovery：启动恢复保留专用重试，不提前退款。

移除400重试前等待在途重试结束；删除 service 模块以及完成处理、后台轮询/超时、启动清理中的对应调用、缓存扩展、内部重放入口。当前首次生成不依赖 `replay_request`，它仍负责400单次重试的参数与顺序还原。海外上传和代理配置可独立保留。本次恢复保持缓存协议与旧、新在途任务兼容；撤销本次局部修改应使用本任务的反向补丁，不整仓回退。

## 验证

2026-09-19 恢复CDN主链路与海外后台旁路：本地业务差异仅限 client.py，未恢复国内上传。核心5个文件106项通过，覆盖CDN首次提交不等待、真实任务号调度、仅海外出口、下载失败/上传失败/部分成功、签名处理图、多图重复顺序、旁路晚到、400等待与单次重提、回调/轮询和积分结算；其他10个受影响模块170项通过，合计276项。日志：`/tmp/kie-cdn-restore-core-tests.log`、`/tmp/kie-cdn-restore-regression.log`。核心首轮仅有沙箱禁止写pytest缓存的警告，测试全部通过；后续验证关闭pytest缓存。未调用真实生成、未提交部署。以下为历史验证快照，不代表当前候选运行机制。

2026-09-18 多图并行：本次只将客户端逐张上传改为最多4张并行，其他生成/重试业务不变。新增乱序完成仍按输入位置返回、4张并发上限、超时与外部取消收束在途/排队任务测试；原测试按文件内容模拟响应，不依赖网络请求到达顺序。4个核心文件共136项通过，`git diff --check` 通过。日志：`/tmp/kie-parallel-upload-tests.log`。未调用真实生成接口，未提交或部署。

2026-09-18 首次生成前置海外上传：本地业务修改限于 KIE 客户端，复用现有上传和 `replay_request`；400重试 service/request、缓存协议、代理配置、Handler及结算代码未改。核心4个文件133项通过，受影响10个文件137项通过，合计270项；覆盖真实Handler调用链的回调/轮询、前置等待占位符、整组CDN降级、上传总超时取消、重复位置与参数、并发隔离、网络重发不重复上传、真实任务号缓存绑定、Redis不可用、400重试和结算。日志：`/tmp/kie-input-preparation-tests.log`、`/tmp/kie-input-preparation-regression.log`。`git diff --check` 通过；仅模拟外部边界，未调用真实生成接口、未提交或部署。

2026-09-18 移除国内旁路：业务代码仅删除 `route=auto` 的调度调用，保留海外上传和共用读取/上传实现。六个 KIE 定向测试文件共 130 项通过，包含回调/轮询链路模拟、仅启动海外上传、代理变量空缺时不降级国内上传、图片顺序/权限、缓存和单次兜底结算；`git diff --check` 通过。日志：`/tmp/kie-domestic-removal-tests.log`。仅本地模拟，未调用真实生成接口、未提交或部署；以下保留历史验证记录。

真实适配器配合模拟 HTTP/Redis/数据库，覆盖实际参数保持、旧缓存兼容、晚到与部分失败、多图顺序、重复通知、提交结果不确定不重发、绑定失败日志、旧超时竞争、积分不重复处理、前端生成状态及无重复上传。另检查受影响的 KIE、重试、任务完成、批次和后台清理测试；不调用生产生成接口。

2026-09-08 精简后本地验证：上述 12 个测试文件共 185 项全部通过，`git diff --check` 通过。日志：`/tmp/kie-fallback-lean-final-20260908.log`。使用隔离测试配置和模拟外部接口，未执行真实付费生成、提交或生产部署。

2026-09-08 工作区取图修改：KIE 旁路、请求重试、缓存、JSON 客户端和媒体 URL 共 5 个测试文件 96 项通过。覆盖两条出口不变、三种图片输入字段、同名不同文件、混合来源顺序、重复位置、中间读取/上传失败不缓存残缺映射、无 CDN 下载、路径安全与大小限制；`git diff --check` 通过。日志：`/tmp/kie-workspace-tests-20260908.log`、`/tmp/kie-workspace-regression-20260908.log`。仅本地模拟验证，尚未提交部署。

后续边界修复：补齐缺身份、跨用户/组织、个人和频道工作区、路径规范化边界、安全 query/fragment、处理/版本参数拒绝与工厂身份透传测试，并回归图片 Handler、换模型、工具、电商及工厂入口。10 个文件 248 项通过（首轮 247 项通过，1 项新增测试的模拟响应漏填 `msg`，补齐测试数据后定向复验通过）。日志：`/tmp/kie-workspace-owner-tests-20260908.log`、`/tmp/kie-workspace-owner-recheck-20260908.log`。独立静态安全复查未发现新的高置信问题；未访问生产，尚未提交部署。

## 发布入口兼容同步

2026-09-08 经用户确认，仅从本地 `prompt-framework-coherence` 任务同步 `deploy/release.sh`、`deploy/deploy.sh`、`deploy/release-coordination.sh` 及两个发布测试文件，补齐发布协议 v2 的生产锁、候选失效/重建和验收一致性保护。未同步其他任务的规则、业务代码或工作树取消功能，未修改来源工作树。发布协调 10 项测试、发布验收生命周期测试和 Shell 语法检查通过，均使用临时本地仓库和模拟 SSH，不访问生产。
