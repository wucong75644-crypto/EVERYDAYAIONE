# FastAPI 框架
fastapi==0.115.6
uvicorn[standard]==0.34.0

# PostgreSQL 客户端（LocalDB 兼容层）
psycopg[binary,pool]==3.3.3

# 配置管理
pydantic==2.10.4
pydantic-settings==2.7.1

# JWT 认证
python-jose[cryptography]==3.3.0
bcrypt==4.2.1

# 日志
loguru==0.7.3
sentry-sdk==2.52.0

# HTTP 客户端
httpx[socks]==0.28.1
socksio==1.0.0

# 阿里云 SDK（短信、OSS）
alibabacloud-dysmsapi20170525==3.0.0
oss2==2.19.1

# Redis 客户端
redis==5.2.1
certifi==2026.1.4

# 重试机制
tenacity==9.0.0

# Google Gemini API
google-genai==1.0.0

# API 限流
slowapi==0.1.9

# AI 记忆层（开源自部署，数据存自有 PostgreSQL + pgvector）
mem0ai==1.0.4
psycopg[pool]==3.3.3

# 企业微信
websockets==14.2
pycryptodome==3.21.0

# 文件解析（企微文件消息）
python-docx==1.2.0
openpyxl==3.1.5

# DuckDB（嵌入式 OLAP 引擎，大报表导出用）
duckdb==1.2.2

# 数据分析（沙盒代码执行）
pandas==2.2.3
pyarrow==23.0.1
python-calamine==0.6.2
fastexcel==0.20.2
matplotlib==3.10.8
seaborn==0.13.2
Pillow==12.2.0
XlsxWriter==3.2.9

# 文档生成（沙盒内 PDF/PPT）
reportlab==4.4.10
python-pptx==1.0.2

# 时间事实层（设计文档：docs/document/TECH_ERP时间准确性架构.md）
tzdata==2024.2                  # IANA 时区数据库（容器内兜底）
chinese-calendar==1.11.0        # 中国法定节假日 + 调休 (覆盖到 2026)。每年 12 月需手动升级到次年版本

# 定时任务（cron 表达式解析）
croniter==2.0.7

# 测试
pytest==8.3.4
pytest-asyncio==0.25.2
time-machine==2.14.1            # 时间冻结（asyncio + zoneinfo + Pydantic v2 兼容）
jieba==0.42.1
langfuse==2.51.3                # v6: Agent 可观测性（trace + span + generation + dashboard）

# ============================================================
# 沙盒（code_execute）数据科学栈 — 对齐 OpenAI Code Interpreter
# AI 在沙盒里写 Python 处理数据/文件常用库，避免 ImportError 翻车
# ============================================================

# 必装（高频）
tabulate==0.9.0                 # pandas.to_markdown() 硬依赖
scipy==1.14.1                   # 统计/插值，scikit-learn 隐式依赖
scikit-learn==1.6.0             # 聚类/分类/回归（客户分群、ABC、异常订单）
chardet==5.2.0                  # 自动识别 CSV 编码（GBK/GB18030 中文电商常见）
python-dateutil==2.9.0          # 解析"2026年3月15日"等中文日期
lxml==5.3.0                     # pd.read_html、XML 解析，比 ElementTree 快 10×
beautifulsoup4==4.12.3          # HTML 清洗
xlrd==2.0.1                     # 读 .xls 老格式（淘宝/拼多多旧版导出常见）

# 可选（中频）
regex==2024.11.6                # 增强 Unicode 中文正则
rapidfuzz==3.10.1               # 商品名/客户名模糊匹配
pyyaml==6.0.2                   # YAML 配置/规则文件
pypdf==5.1.0                    # PDF 合并/拆分（已有 pdfplumber 只读，pypdf 可写）
psutil==6.1.1                   # 沙盒内监控内存（防 OOM）
