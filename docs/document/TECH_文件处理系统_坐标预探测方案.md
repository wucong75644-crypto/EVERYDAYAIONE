# 文件处理系统 — 坐标预探测方案

> 基于 TECH_文件处理系统.md 的补充方案
> 创建：2026-05-16
> 状态：方案讨论，待确认

---

## 一、核心架构变化

```
原来：代码写规则检测 → 20 个异常点 → 规则写不完
现在：坐标预探测（前50行）→ AI 一次判断结构 → 按结论读文件 → 写 Parquet
```

---

## 二、方案审核意见

### 整体判断：方案正确，3 处需要调整

**方案的核心价值成立**：用 1 次 AI 调用替代 20 个检测规则，成本低（¥0.01-0.03/次）、覆盖全、可缓存。

---

### 调整 1：预探测不只看前 50 行，要三段采样

你方案写的：
```
读前 50 行（开头+中间+末尾各抽样）
```

但括号里的"中间+末尾各抽样"和"前50行"矛盾。应该明确：

```
预探测采样策略（复用已有的三段扫描思路）：
  开头 20 行：表头区 + 前几行数据
  中间 10 行：数据区中段（格式变化检测）
  末尾 20 行：合计行/备注/尾部异常

总共 50 行，但不是连续的前 50 行。
```

这跟三段扫描一致，不是新概念。

---

### 调整 2：AI 判断的输入格式需要精确定义

你方案写了 AI 返回的 schema，但没定义**输入给 AI 看的坐标格式**。AI 要能判断结构，输入必须包含：

```
给 AI 的输入（坐标格式）：

文件: 2026公摊明细表4月.xlsx | Sheet: 公摊明细 | 总行数: 56

合并单元格：
  A1:C1 = "3月"
  D1:E1 = "4月"

采样数据（带行列坐标）：
  Row 0: [A:"3月", B:null, C:null, D:"4月", E:null]
  Row 1: [A:"费用明细", B:"按年摊销金额", C:"费用金额", D:"按年摊销金额", E:"费用金额"]
  Row 2: [A:"金华写字楼租金", B:67630.55, C:5635.88, D:"金华写字楼租金", E:5635.88]
  ...
  Row 25: [A:"公摊", B:null, C:2.16, D:null, E:2.50]
  Row 26-29: [全空]
  ...
  Row 53: [A:"剔除", B:null, C:null, D:null, E:null]
  Row 54: [A:"xxx", B:123, C:456, D:null, E:null]

请判断：表头位置、数据区范围、合并语义、特殊行。
```

**关键：坐标格式必须带 null 标记**，AI 才能看出合并单元格的空值位置。

---

### 调整 3：A 类和 B 类的边界需要重新划分

你的分类里，有些归到 A 类（需要 AI）的其实 B 类代码也能做：

| 异常点 | 你归的类 | 我的建议 | 原因 |
|--------|---------|---------|------|
| #3 多级表头 | A（AI 判断） | **A 正确** | 语义理解，代码判断不了 |
| #4 无表头文件 | A（AI 判断） | **B 可做** | `detect_header_row` 返回 0 就是无表头，不需要 AI |
| #5 表头在中间 | A（AI 判断） | **B 已做** | `detect_header_row` 已能定位中间表头 |
| #6 多张表拼一起 | A（AI 判断） | **B 已做** | `detect_table_regions` 已实现 |
| #7 区域边界模糊 | A（AI 判断） | **A 正确** | 1 行空行是分隔还是缺失，需要语义判断 |
| #8 横向多区域 | A（AI 判断） | **A 正确** | 代码检测不了横向分割 |
| #18 合计行 | B（代码处理） | **A 更好** | "合计"关键词不可靠（可能是列名/商品名含"合计"） |

调整后：

```
A 类（必须 AI 判断）：8 个
  #3 多级表头、#7 区域边界模糊、#8 横向多区域
  #9 备注区域、#15 纵向合并方向、#16 横向合并语义
  #17 嵌套合并、#18 合计行、#19 单位行、#20 斜线表头

B 类（代码已能处理）：5 个
  #1 全空行、#2 全空列、#4 无表头、#5 表头在中间、#6 多张表拼一起
  + 重复列名去重、多级表头展平（Parquet 技术要求）

C 类（三段扫描）：3 个
  #10 日期格式、#12 货币百分比、#13 混合类型
  + 前置零订单号、超大数字精度
```

---

## 三、实施路径建议

```
你的方案：
  第一批（1周）：B 类 bug 修复
  第二批（1周）：坐标预探测 + AI 判断
  第三批（3天）：三段扫描接入

我的建议：
  第一批已完成 ✅（B 类全部改完 + issues 统一 schema + 20/20 通过）
  
  第二批开始前，需要先确认：
  1. AI 探测用哪个模型？（qwen-turbo 够吗？还是需要 plus？）
  2. 探测结果缓存在哪里？（meta.json 里加 structure_analysis 字段？）
  3. 探测失败怎么降级？（退回纯代码检测？）
  
  这三个决策确认后再开始第二批。
```

---

## 四、成本估算补充

你的估算基本正确，补充一点：

```
你算的：2000-3000 tokens / 次 → ¥0.01-0.03
实际可能：
  50 行 × 20 列 × 平均 8 字符/值 = 8000 字符输入
  + 系统提示词 ≈ 1000 字符
  + 合并信息 ≈ 500 字符
  合计输入 ≈ 4000 tokens
  
  AI 返回结构化 JSON ≈ 500 tokens
  
  qwen-turbo：输入 ¥0.001/千 + 输出 ¥0.002/千
  单次成本：¥0.004 + ¥0.001 = ¥0.005
  
  每天 100 次：¥0.5 / 天
  → 比你估算的还低
```

---

## 五、与现有代码的关系

```
保留（不动）：
  ✓ detect_header_row（值内容模式匹配）→ B 类兜底
  ✓ detect_table_regions → B 类兜底
  ✓ 三段扫描 _prescan_schema → C 类
  ✓ _cast_to_schema → 分块写入
  ✓ 统一 issues schema → 所有标注

新增：
  + 坐标预探测模块（采样 + 格式化）
  + AI 结构判断（调用 LLM + 解析返回）
  + 结论缓存（写入 meta.json）
  + 探测失败降级（退回代码检测）

关系：
  AI 探测成功 → 用 AI 的结论（header_rows / data_start_row / column_mapping）
  AI 探测失败 → 退回 detect_header_row（现有逻辑）
  两条路径输出格式一致 → 下游不感知

prescan 各字段的使用方式：
  header_rows / data_start_row → 驱动 L1 管道（替代 detect_header_row）
  column_mapping → 驱动列名重命名（单表格路径 + 多区域路径）
  regions → 只写入 meta.json 供 AI 业务层参考（不驱动多区域检测）
  special_rows → 只写入 meta.json 供 AI 业务层参考（不驱动代码逻辑）

多区域检测始终由 detect_table_regions 代码执行（扫 5000 行，比 AI 50 行采样更准）
```

---

## 六、审核修复记录（2026-05-16）

### 修复 #1：prescan regions 不驱动多区域检测

```
经验教训：AI 的 regions 基于 50 行采样推断，不够准确。
实测中 AI 把发票文件误判为多区域，导致数据丢失（1171 行→45 行）。

结论：regions 只写入 meta.json 给 AI 业务层看，不用来驱动 L1 管道。
多区域检测由 detect_table_regions 代码执行（扫 5000 行更可靠）。
```

### 修复 #2：column_mapping 覆盖多区域分支

```
convert_multi_region 新增 column_mapping 参数，合并完成后统一 rename。
普适方案——column_mapping 为空时不执行 rename，不影响原有逻辑。
```

### 修复 #5：L2 触发条件扩展

```
status=warning + confidence < 0.7 → 也触发 L2
```

### 修复 #6：缓存失效机制

```
已有 .snapshot 的 mtime+size 比对。prescan 结论随 meta.json 一起失效。
```
