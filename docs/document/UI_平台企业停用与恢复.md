# 平台企业停用与恢复 UI

- 企业行持续展示服务器返回的 `active` / `suspended` 权威状态。
- active 仅显示“停用”，suspended 仅显示“恢复”，不提供删除入口。
- 停用弹窗展示企业名、服务不可用但数据保留的影响，并要求精确输入完整企业名。
- 恢复弹窗说明 active 成员恢复原角色能力，disabled 成员和失效 Secret 不自动恢复。
- 提交期间禁用关闭和按钮；成功后重新加载企业列表，不做乐观状态切换。
- 列表和转换请求可取消，组件卸载及过期响应不得写入界面；失败保留旧状态并允许重试。
