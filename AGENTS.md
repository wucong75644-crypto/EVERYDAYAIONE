# EVERYDAYAIONE AI 开发规则（V4.0）

> 用户是最终决策者。本文件只保存项目级长期门禁；阶段流程由对应 Skill 承担。

## 1. 核心原则

- 先理解再修改：先查现有实现、调用方、数据源和相似逻辑。
- 最小充分：只改当前目标需要的内容，不做投机功能、顺手优化或无关重构。
- 保护现场：用户已有修改、暂存内容和未跟踪文件不得覆盖、清理或混入任务。
- 证据闭环：完成必须有与风险匹配的测试、检查或运行证据。
- 用户决策：会改变产品含义、范围、公共契约、长期成本或风险接受的决定必须由用户确认。

## 2. 任务等级

- A级：改变产品行为、公共契约、架构、数据库结构、权限、安全或核心状态。先完成影响分析和方案，等待用户确认后实施。
- B级：范围明确、可逆、无公共契约变化的小型代码或文档修改。可在说明理解后直接实施。
- C级：Git、依赖、环境、部署、重启、已确认迁移的执行等操作任务。按对应运维 Skill 和失败关闭门禁执行。

文件数量只用于估算范围，不单独决定等级。编写或修改数据库 migration 属于 A级；执行已经确认的 migration 属于 C级。

## 3. 变更门禁

- 修改前确认任务范围、完成条件及受影响调用链。
- A级未经确认不得修改代码、迁移或运行行为。
- 新证据推翻方案、需要扩大范围或产生重大风险时立即停止并报告。
- 不得擅自改变 API、数据库、目录、权限或产品交互。
- 文件移动必须更新真实引用并验证旧路径残留；只有存在真实兼容调用方时才保留 re-export。
- 不得执行未获授权的推送、部署、生产写入或不可逆操作。

## 4. 单任务执行协议

- 一个用户任务默认由同一主 Agent 连续完成；Skill 是按需采用的方法，不是必须拆开的子任务或阶段接力。
- A级或复杂任务先完成集中调查，再进入实施。调查阶段必须形成最小任务地图：完成条件、相关文件与符号、主要调用链、现有测试与运行约束、计划修改清单、实施批次和验证计划；任务地图完成前不得编辑代码。
- 集中调查不是强制一次读取全部文件。先通过目录、符号、调用方和目标片段建立整体结构；局部证据不足以确认调用链、边界或方案时继续扩大，必要时读取全文。
- A级任务在任务地图和方案经用户确认后实施。B级任务可以在同一轮形成简短内部任务地图后直接实施；C级任务先完成一次环境与目标预检，再执行既有操作流程。
- 实施按已经规划的完整逻辑批次连续推进。禁止把“读取一个文件、修改一点、测试一次”作为默认循环；只有新证据推翻计划、暴露未知依赖或使现有证据失效时才返回调查。
- 同一任务复用已经取得的范围、文件、调用链、根因、设计、diff 和测试证据；不得仅因切换 Skill 重复读取、重新建模或重新检查。
- 代码、配置、依赖、环境、任务范围或 commit SHA 变化，或者现有证据不足时，只有受影响的证据失效，再定向补充调查或验证。
- 无依赖的读取和检查优先在同一轮并行或批量完成；连续机械步骤优先通过一次工具调用或项目脚本完成。
- 本项目默认单次工具结果不超过约 10K Token。禁止一次拼接多个完整大文件、完整测试日志或大范围 diff 返回主上下文；若限制导致证据不足，必须继续读取相关范围，必要时读取全文。
- 命令输出过大时保留完整结果到临时文件或日志，只返回摘要与原始结果位置；失败时再定向展开关键部分。
- 修改完成后先确定一次验证漏斗，再按“静态/结构检查 → 定向测试 → 必要时扩大”执行；同一代码状态下已经通过且仍覆盖风险的验证不得重复运行。
- 中间过程只在需要用户决策、出现阻塞、范围变化或重大新风险时汇报；正常阶段转换不单独输出。
- B级任务由主 Implementation 或 Testing 在同一执行轮完成实施、验证和自审；不为流程形式单独加载 Test Coverage 或 Review。数据库、安全、权限、核心状态和生产事故等高风险任务可使用独立验证或审查。
- 多 Agent 只用于能够隔离大量噪声的独立工作或高风险审查，不用于需求、设计、实施、测试、Review之间的常规接力。

## 5. 能力路由

命中场景时读取并执行对应 Skill：

- 新功能或行为调整：`source-command-everydayai-requirement`
- 行业标准、成熟机制不明确：`source-command-everydayai-industry-analysis`
- 存在实质方案取舍：`source-command-everydayai-evaluate`
- UI、交互或信息呈现：`source-command-everydayai-ui-design`
- 技术方案或架构设计：`source-command-everydayai-tech-design`
- 代码开发：`source-command-everydayai-implementation`
- Bug、异常或生产故障：`source-command-everydayai-testing`
- 用户明确要求测试，或主流程发现验证证据不足：`source-command-everydayai-test-coverage`
- 用户明确要求独立审查，或高风险变更需要独立复核：`source-command-everydayai-review`
- 推送、部署、重启：对应运维 Skill

Skill 负责按需方法，本文件负责不可绕过的全局门禁。Implementation 和 Testing 对普通任务的验证与自审负责；只有用户明确要求、A级或高风险、证据不足，或独立性会显著提高质量时，才单独使用 Test Coverage 或 Review。

## 6. 质量底线

- 新增或改动后的文件原则上不超过 500 行，函数不超过 120 行，复杂度不超过 15，嵌套不超过 4 层。
- 既有文件已超阈值时，不得继续恶化；是否拆分由当前风险和任务范围决定，不能借机扩大重构。
- 不新增 TODO、workaround、死代码、无用兼容层或硬编码敏感信息。
- 公共接口保持兼容；明确批准的版本升级、迁移或 contract 阶段除外。
- 无关代码异味只报告，不擅自修复。

优先运行 `python3 scripts/check_task_change.py --files ...` 检查可机械验证项；语言和目录专用规则由就近 `AGENTS.md` 补充。

## 7. 验证与文档

- 测试按定向验证、受影响模块、必要时扩大执行；不默认运行全量或 External 测试。
- 大型文件默认渐进式读取：先通过 `rg`、目录、标题、符号或目标路径定位相关段落；局部内容不足以确认调用链、上下文、边界或结论时逐步扩大范围，必要时读取全文。不得为节省 Token 牺牲正确性和完整性。
- Bug 修复必须验证根因对应的系统约束，不能只证明表面症状消失。
- 数据库变更必须有 migration、rollback、兼容与回滚分析；禁止直接修改生产 Schema。
- 新增、删除、移动文件时更新项目概览；公共函数签名变化时更新函数索引；架构和阶段结论更新对应文档。
- 文档未触发时无需为汇报形式制造修改。

## 8. Git、发布与安全

- 只提交当前任务明确文件，禁止 `git add .`、`git add -A` 或覆盖用户工作。
- Commit 使用 `<type>: <description>`，type 为 `feat|fix|refactor|docs|test|chore|perf|ci`。
- `.env`、密钥、Token、证书、本地依赖、缓存和输出目录不得提交。
- 新增环境变量时同步 `.env.example`，只写示例值。
- 推送和部署正常路径不重复询问；已有暂存、敏感命中、测试失败、远端漂移或健康检查失败时必须停止。
- 未经明确授权，不得以验证为目的产生生产业务副作用。人工业务验收未完成不构成技术失败，也不得触发重复提交、推送或部署。
- 生产操作必须使用项目既有脚本和精确目标，不得绕过门禁。

## 9. 增量精简输出

- 默认只输出：结论、证据摘要、风险或阻塞、下一步。
- 不复述用户需求、历史背景、已确认方案或完整工具日志。
- 只报告本轮新增信息；成功验证合并为“命令/范围 + 结果”，失败才展开关键错误。
- 通过文件路径、commit SHA 和工具记录引用详细证据，不在对话中重复粘贴。
- 普通最终回复默认控制在 300 字内。
- 提交给用户的方案必须完整，包含目标、范围、推荐方案、关键依据与取舍、影响、风险、验证方式和待确认事项，不受普通回复长度限制。
- 任务完成后的汇报默认简洁，不因任务等级高而自动展开。只有用户明确要求详细或自包含内容，或者存在失败、生产状态不确定、数据或安全风险时才展开。
- 精简只影响展示，不得削减必要调查、实现、验证或风险披露。
