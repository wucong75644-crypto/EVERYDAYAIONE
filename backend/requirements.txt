# FastAPI 框架
fastapi==0.115.6
uvicorn[standard]==0.34.0

# PostgreSQL 客户端（LocalDB 兼容层）
psycopg[binary,pool]==3.3.3

# 配置管理
pydantic==2.10.4
pydantic-settings==2.7.1

# JWT 认证
python-jose[cryptography]==3.3.0
bcrypt==4.2.1

# 日志
loguru==0.7.3
sentry-sdk==2.52.0

# HTTP 客户端
httpx[socks]==0.28.1
socksio==1.0.0

# 阿里云 SDK（短信、OSS）
alibabacloud-dysmsapi20170525==3.0.0
oss2==2.19.1

# Redis 客户端
redis==5.2.1
certifi==2026.1.4

# 重试机制
tenacity==9.0.0

# Google Gemini API
google-genai==1.0.0

# API 限流
slowapi==0.1.9

# AI 记忆层（开源自部署，数据存自有 PostgreSQL + pgvector）
mem0ai==1.0.4
psycopg[pool]==3.3.3

# 企业微信
websockets==14.2
pycryptodome==3.21.0

# 文件解析（企微文件消息）
PyPDF2==3.0.1
python-docx==1.2.0
openpyxl==3.1.5

# 数据分析（沙盒代码执行）
pandas==2.2.3
pyarrow==23.0.1

# 时间事实层（设计文档：docs/document/TECH_ERP时间准确性架构.md）
tzdata==2024.2                  # IANA 时区数据库（容器内兜底）
chinese-calendar==1.11.0        # 中国法定节假日 + 调休 (覆盖到 2026)。每年 12 月需手动升级到次年版本

# 定时任务（cron 表达式解析）
croniter==2.0.7

# 测试
pytest==8.3.4
pytest-asyncio==0.25.2
time-machine==2.14.1            # 时间冻结（asyncio + zoneinfo + Pydantic v2 兼容）
jieba==0.42.1
